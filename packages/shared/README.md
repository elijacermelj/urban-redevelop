# @urban/shared

Shared contracts and types for frontend and backend.

## What's inside
- `src/types.ts`: shared TypeScript domain types
- `src/contracts.ts`: shared Zod schemas for runtime validation

## Usage
Import from web/api packages using:
- `@urban/shared/types`
- `@urban/shared/contracts`
